# Global composer packages
composer_packages:
  - "{{ 'laravel/valet' if is_mac_os else 'cpriego/valet-linux' }}"
  - squizlabs/php_codesniffer
  - friendsofphp/php-cs-fixer

# Global pip (python) packages
pip_packages:
  - pipenv
  - ansible
